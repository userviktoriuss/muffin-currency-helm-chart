apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "muffin-wallet-chart.fullname" . }}-test-connection"
  labels:
    {{- include "muffin-wallet-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "muffin-wallet-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
